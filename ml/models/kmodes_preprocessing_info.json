{
  "description": "K-Modes input preprocessing",
  "steps": [
    "Select the configured clustering features",
    "Convert each feature to integer nullable dtype",
    "Convert each feature to string",
    "Pass the resulting array to kmodes_model.predict()",
    "Add the configured label offset because the saved K-Modes model returns zero-based labels"
  ],
  "n_features": 33,
  "model_input_type": "categorical strings",
  "k": 2,
  "random_state": 43,
  "label_offset": 1,
  "segment_label_strategy": "auto-labeled from modal profile engagement score",
  "negative_vars": [
    "tech_issues_freq"
  ]
}